version: 1.2.8
cache: true

# ----------------------------------------------------------------------------
# GenevIA Workspace — configuration LibreChat
# Inference: Infomaniak AI (Suisse), API compatible OpenAI
# product_id: 109317
# ----------------------------------------------------------------------------

interface:
  customWelcome: "Bienvenue sur GenevIA, {{user.name}}. Votre IA pense en Suisse."
  # On masque les reglages avances pour eviter la paralysie du choix
  parameters: false
  presets: false

endpoints:
  custom:
    - name: 'GenevIA'
      apiKey: '${INFOMANIAK_API_KEY}'
      baseURL: 'https://api.infomaniak.com/2/ai/${PRODUCT_ID}/openai/v1'
      models:
        # Seuls ces 2 modeles sont visibles cote utilisateur.
        # Le premier (Apertus) est le defaut.
        default:
          - 'swiss-ai/Apertus-70B-Instruct-2509'
          - 'mistralai/Mistral-Small-4-119B-2603'
        fetch: false
      titleConvo: true
      # Modele bon marche pour generer les titres de conversation (protege la marge)
      titleModel: 'mistralai/Ministral-3-14B-Instruct-2512'
      modelDisplayLabel: 'GenevIA'
      iconURL: ''
